server:
  port: 8080

spring:
  application:
    name: liyi_music
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://${MYSQL_HOST}:${MYSQL_PORT}/${DATABASE_NAME}?useSSL=true&useUnicode=true&characterEncoding=UTF-8&useTimezone=true&serverTimezone=GMT%2B8&allowMultiQueries=true
    username: ${MYSQL_USER_NAME}
    password: ${MYSQL_USER_PASSWORD}